<UserControl x:Class="Watch3D.Gui.MeshPropertiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Watch3D.Gui"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0">Mesh</TextBlock>
        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Path=Name, Mode=OneWay}" />
        <TextBlock Grid.Row="1" Grid.Column="0">Front color</TextBlock>
        <xctk:ColorPicker Grid.Row="1" Grid.Column="1" SelectedColor="{Binding Path=FrontSurfaceColor}"/>
        <TextBlock Grid.Row="2" Grid.Column="0">Back color</TextBlock>
        <xctk:ColorPicker Grid.Row="2" Grid.Column="1" SelectedColor="{Binding Path=BackSurfaceColor}"/>
    </Grid>
</UserControl>
